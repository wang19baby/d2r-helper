# 开心邪帝.d2s 二进制结构验证

本文只记录已经通过真实文件 `D:\work_space\personal_workspace\d2r\开心邪帝.d2s` 字节验证的结论，不把推测写成既定事实。

## 样本信息

- 文件路径：`D:\work_space\personal_workspace\d2r\开心邪帝.d2s`
- 文件长度：`4126` bytes
- SHA256：`A239A970D9371C0B2D9175D0A25F8C71EBCA84521CF12788D39C80EBE0FECEE2`
- 与仓库样本 `src-tauri/tests/fixtures/xieedi.d2s` 字节完全一致

## 已验证的顶层分段

下列 marker 和偏移，已经直接从真实字节验证：

| 偏移 | 原始字节 | 说明 |
|---|---|---|
| `0x0000` | `55 AA 55 AA` | 文件 magic |
| `0x0341` | `67 66` | attributes 段，`gf` |
| `0x0374` | `69 66` | mercenary 段，`if` |
| `0x0394` | `4A 4D 68 00` | items 主段，`JM` + `count=104` |
| `0x0DD3` | `4A 4D 00 00` | 空 `JM` 结束段 |
| `0x0DD7` | `6A 66` | `jf` 段 |
| `0x0FA1` | `6B 66` | `kf` 段 |
| `0x1006` | `67 66` | 另一个 `gf` 段 |

对应的十六进制片段：

```text
0x0341: 67 66 00 1E 09 40 81 80 E7 06 A0 68 00 87 6C 1D
0x0374: 69 66 01 14 01 01 05 14 01 01 01 14 14 14 14 01
0x0394: 4A 4D 68 00 10 00 80 00 05 4C 47 D8 6D 28 13 50
0x0DD3: 4A 4D 00 00 6A 66 4A 4D 09 00 10 08 80 04 CD 0C
0x0DD7: 6A 66 4A 4D 09 00 10 08 80 04 CD 0C 80 9B D9 A8
0x0FA1: 6B 66 00 01 00 6C 66 01 00 18 00 02 00 2A 00 A8
0x1006: 67 66 06 00 40 9C 00 0E 00 80 38 01 30 C0 02 00
```

## 当前可确认的文件结构

```rust
struct D2SFileVerified {
    header: D2SHeaderVerified,
    character_section: [u8; 0x183],   // 0x010..0x192
    quests_section: [u8; 0x12A],      // 0x193..0x2BC
    waypoints_section: [u8; 0x50],    // 0x2BD..0x30C
    npcs_section: [u8; 0x34],         // 0x30D..0x340
    attributes: AttributesSection,    // 0x341..?
    mercenary: MercenarySection,      // 0x374..0x393
    items_jm: JMSection,              // 0x394..0x0DD2
    items_jm_end: [u8; 4],            // 0x0DD3..0x0DD6
    journey_jf: SectionJf,            // 0x0DD7..0x0FA0
    corpse_kf: SectionKf,             // 0x0FA1..0x1005
    final_gf: SectionGf,              // 0x1006..EOF
}
```

## Header 已验证字段

| 偏移 | 长度 | 字段 | 值 |
|---|---|---|---|
| `0x00` | 4 | magic | `55 AA 55 AA` |
| `0x04` | 4 | version | `105` |
| `0x08` | 4 | filesize | `4126` |
| `0x0C` | 4 | checksum | `166156248` |
| `0x10` | 4 | active_weapon | `1` |
| `0x14` | 1 | status_flags | `0x08` |
| `0x18` | 1 | class | `7` |
| `0x1B` | 1 | level | `116` |
| `0x1C` | 4 | unknown_or_time | `0x00000000` |
| `0x12B` 起 | var | UTF-8 名字 | `开心邪帝` |

## Attributes 段已验证结论

- 起点：`0x0341`
- marker：`67 66`，即 `gf`
- 编码方式：`9-bit stat_id + N-bit value`，`0x1FF` 终止
- 该样本已直接解出的值：

| 字段 | raw | display |
|---|---:|---:|
| strength | 143 | 143 |
| energy | 20 | 20 |
| dexterity | 463 | 463 |
| vitality | 552 | 552 |
| hitpoints | 746552 | 2916 |
| max_hp | 544512 | 2127 |
| mana | 132835 | 519 |
| max_mana | 49280 | 192 |
| stamina | 222464 | 869 |
| max_stamina | 203520 | 795 |
| level | 116 | 116 |
| experience | 3748370191 | 3748370191 |
| gold | 43607 | 43607 |
| gold_bank | 2411887 | 2411887 |

说明：

- `hitpoints/max_hp/mana/max_mana/stamina/max_stamina` 是 Q8 值，显示值为 `raw / 256`
- 这个 attributes 结论已经在真实字节上复算过

## JM 主区已验证结论

### JM 段头

| 偏移 | 长度 | 字段 | 值 |
|---|---|---|---|
| `0x394` | 2 | marker | `JM` |
| `0x396` | 2 | item_count | `0x0068 = 104` |
| `0x398` | - | bitstream 起点 | item 数据流 |

### code table 生成规则已验证

项目里的 `819` 项 code table 并不是随手拼出来的，当前已经确认：

- 生成脚本 [generate_tables.cjs](file:///d:/work_space/personal_workspace/d2r/d2r-marketplace-tauri/tools/generate_tables.cjs#L121-L123) 会对 `ALL_ITEMS` 做 `code.localeCompare()` 排序
- 也就是说，当前分析使用的 `code_table_tmp.txt` 这条“按 code 字典序排序”的假设是有脚本依据的
- 对照的“未排序表”来自 [generate_game_items.rs.js](file:///d:/work_space/personal_workspace/d2r/d2r-marketplace-tauri/tools/generate_game_items.rs.js#L16-L21)，顺序是 `armor_items -> weapon_items -> other_items`
- 已做过比对，`未排序表` 下目标物品索引例如：
  - `uit = 141`
  - `uap = 116`
  - `rin = 522`
  - `amu = 520`
  - `7s8 = 457`
- 这些值和当前在 `JM` header 中读到的关键候选值不匹配，因此“未排序表”目前可以先排除

### `code index` 位窗并非完全坐实

虽然当前文档默认使用 `76..85` 作为 `10-bit code index`，但继续做全局统计后发现：

- 如果固定使用 `76..85`
- 再叠加：
  - `parent = 1`
  - `equipped in 1..12`
  - `ilvl <= 120`
  - `quality in 1..8`
  - `槽位类别匹配`

那么整个 `JM` 主区中，各槽位命中分布是：

| equipped | 命中数 |
|---:|---:|
| 1 | 4 |
| 2 | 0 |
| 3 | 0 |
| 4 | 22 |
| 5 | 0 |
| 6 | 0 |
| 7 | 0 |
| 8 | 0 |
| 9 | 0 |
| 10 | 0 |
| 11 | 8 |
| 12 | 4 |

这和角色已知真实装备明显矛盾：

- 你明确有项链、双戒指、腰带、鞋子、手套、盾牌
- 但 `76..85` 模型下这些槽位一个都打不出来

因此目前结论应改成：

- `76..85` 仍然是一个**强候选**
- 但它**还不能视为对所有 item/所有槽位都完全成立**

### 已验证的 item header 位段

下列位段在真实字节滑窗中可以稳定命中大量候选 header，说明这组定义大方向成立：

| 位范围 | 字段 |
|---|---|
| `20` | identified |
| `37` | simple flag |
| `38` | ethereal |
| `40` | personalized |
| `42` | runeword |
| `58..60` | parent |
| `61..64` | equipped slot |
| `65..68` | column |
| `69..71` | row |
| `73..75` | stash |
| `76..85` | 10-bit code index |

### Body slot 当前采用的真实枚举

| 值 | 槽位 |
|---:|---|
| 1 | Head |
| 2 | Neck |
| 3 | Torso |
| 4 | Right Hand |
| 5 | Left Hand |
| 6 | Right Ring |
| 7 | Left Ring |
| 8 | Waist |
| 9 | Feet |
| 10 | Hands |
| 11 | Alt Right Weapon |
| 12 | Alt Left Weapon |

## 已在真实字节中单点复算的候选 header

这些位置不是“最终已确认 item”，只是已做过位段复算的样本点：

### 候选 A

- 起点：`byte 2167`
- 原始 14 字节：

```text
00 10 00 A0 08 05 BC C4 CA 33 00 10 00 80
```

- 复算结果：

```text
parent = 1
equipped = 6
col = 5
row = 6
stash = 1
code_index = 3
code = 6bs
simple = 0
identified = 0
ethereal = 0
personalized = 1
runeword = 1
```

这和之前文档里“右戒指位置出现 `6bs`”的现象一致，说明：

- `parent/equipped/code index` 这几个位段很可能是对的
- 但 `code index -> code` 的全局表仍需继续校对

### 候选 B

- 起点：`byte 3184`
- 原始 14 字节：

```text
00 00 00 C0 10 05 C8 84 EF 34 84 FB 19 17
```

- 复算结果：

```text
parent = 1
equipped = 12
col = 7
row = 7
stash = 2
code_index = 67
code = 7s8
simple = 0
identified = 0
ethereal = 0
personalized = 1
runeword = 1
```

这里 `code = 7s8` 能和样本中已知武器底材对上，说明 `10-bit code index` 方案至少在部分 item 上是成立的。

### 候选 C

- 起点：`bit 22235`，即 `byte 2779 + bit 3`
- 这是一个**非 byte 对齐**候选点
- 复算结果：

```text
parent = 1
equipped = 4
code_index = 67
code = 7s8
identified = 0
ethereal = 0
personalized = 0
runeword = 0
```

- 如果按“112-bit header 后直接进入 advanced body”的模型继续读：

```text
item_id = 428995066
ilvl = 76
quality = 3
multi_pic = 0
class_specific = 1
```

这个结果明显比前面多个假阳性更像真实 item，原因是：

- `7s8` 本身和角色已知武器底材一致
- `equipped = 4` 正好对应主武器位
- `ilvl = 76`、`quality = 3` 都在合理范围内

因此，当前可以把 `bit 22235` 当作一个**高可信度真实 item 起点样本**。

#### 对 `7s8 @ bit 22235` 的 body 深挖

进一步按 `advanced body` 布局向后读，当前可得到：

```text
item_id        = 428995066
ilvl           = 76
quality        = 3   // Superior
multi_pic      = 0
class_specific = 1
auto_affix     = 645
superior_bits  = 0
max_durability = 128
cur_durability = 64
v105_2         = 0
stat_start     = 22425
```

从 `stat_start = 22425` 往后扫描 `0x1FF` terminator，当前 512 bit 窗口里命中两个候选：

| terminator bit | terminator 后 end bit | byte 对齐后 |
|---:|---:|---:|
| `22604` | `22622` | `22624` |
| `22752` | `22770` | `22776` |

这说明：

- `7s8` 这件 item 的真实结束位目前仍未唯一确定
- 但已经能把候选范围从“整段 JM”收缩到“`22624` 或 `22776` 两个邻近边界”

对这两个可能结束位的后继扫描结果：

- `22624` 附近只出现很弱的候选（如 `glb`, `9fb`），不符合已知装备
- `22776` 附近出现一个较强候选：

```text
bit 22827
eq   = 10
code = 7fb
ilvl = 27
q    = 6
```

但 `7fb = Colossal Sword`，它和 `eq=10(Hands)` 类别不匹配，因此：

- `22776` 还不能直接认定为真实边界
- 也不排除 `0x1FF` 命中的只是 property list 内部的伪终止

### 候选 D

基于同样的“header + plausible body”筛选条件：

- `parent = 1`
- `equipped in 1..12`
- `code_index < 819`
- `ilvl <= 120`
- `quality in 1..8`

当前能稳定复现出以下几个与旧分析一致的样本：

| bit 起点 | 槽位 | code | ilvl | quality |
|---:|---:|---|---:|---:|
| `9020` | `12` | `g33` | `5` | `7` |
| `9588` | `12` | `g33` | `4` | `7` |
| `17336` | `6` | `6bs` | `44` | `7` |
| `22235` | `4` | `7s8` | `76` | `3` |

这说明两点：

1. 旧分析中出现的 `g33 / 6bs / 7s8` 不是完全随机噪声
2. `JM item` 的真实起点很可能经常是 **bit 对齐** 而不是 byte 对齐

### 候选 E

对旧分析里最像“右戒指”的样本 `bit 17336` 继续深挖，得到：

```text
parent = 1
equipped = 6
code = 6bs
quality = 7
item_id = 3224014080
ilvl = 44
multi_pic = 1
picture_id = 1
given_runeword = 1
personalized = 1
unique_id = 1384
runeword_id = 3334
personalized_name(bytes interpreted as ascii) = "^z¤HH"
max_durability = 33
cur_durability = 195
v105_2 = 3
stat_start = 17607
```

这条样本的问题在于：

- `personalized_name` 明显不像真实名字
- `v105_2 = 3` 与 `7s8` 样本的 `0` 不一致，也不符合当前预期
- 它的第一个 terminator 候选对齐到 `17720` 后，附近没有接出明显的下一条可信 item

因此当前判断：

- `bit 17336` 虽然在 header 层面“像一个右戒指位”
- 但它的 body 一致性明显弱于 `bit 22235`
- **暂列为低可信样本，不应当直接当成真实戒指**

### 现成扫描器的适用性限制

仓库里的 [parser_v3.rs](file:///d:/work_space/personal_workspace/d2r/d2r-marketplace-tauri/src-tauri/src/protocol/d2i/parser_v3.rs#L66-L69) 有一个重要限制：

- `try_parse_candidate()` 明确要求 `bit_off % 8 == 0`
- 也就是它只接受 **byte 对齐** 的候选头

而当前最可信的 `7s8` 起点在：

- `bit 22235 = byte 2779 + 3 bit`

这意味着：

- 现成的 `d2i parser_v3` 扫描器不能直接用于这份 `d2s` 的 `JM` 主区
- 后续如果要复用它的思路，只能复用“body 跳过/评分规则”，不能直接复用“byte-aligned candidate”这个前提

### 替代位窗的局部命中

为了验证 `code index` 起始位是否可能有偏移，已对 `70..86` 做过枚举。

在“只看你这套装备最关心的目标 code”时，出现过以下局部命中：

| 位窗起点 | 命中示例 |
|---:|---|
| `70` | `eq=2 -> vip`，`eq=10 -> tgl` |
| `79` | `eq=6 -> rin` |
| `81` | `eq=11 -> wae` |

单点复算结果如下：

#### `bit 10538`, `off=70`

```text
parent = 1
equipped = 2
code = vip
socketed = 1
simple = 0
runeword = 1
item_id = 1778846401
ilvl = 28
quality = 1
```

问题：

- `vip = Amulet of the Viper` 作为项链位本身是合理的
- 但 `runeword = 1`、`quality = 1` 的组合不太像真实亮金/魔法项链

#### `bit 22639`, `off=70`

```text
parent = 1
equipped = 10
code = tgl
socketed = 1
simple = 0
ethereal = 1
personalized = 1
runeword = 1
item_id = 2098174
ilvl = 0
quality = 2
```

问题：

- `tgl = Light Gauntlets / Magefist` 系手套底材，槽位上是对的
- 但 `ilvl = 0` 明显不自然
- 同时出现 `ethereal + personalized + runeword` 也非常可疑

#### `bit 22882`, `off=79`

```text
parent = 1
equipped = 6
code = rin
socketed = 0
simple = 1
runeword = 1
item_id = 536871967
ilvl = 64
quality = 2
```

问题：

- `rin` 落到右戒指位很诱人
- 但 `simple = 1` 基本可以判它不是真实戒指头
- 戒指不应走 simple item 路径

#### `bit 25190`, `off=81`

```text
parent = 1
equipped = 11
code = wae
simple = 0
runeword = 0
item_id = 1308862559
ilvl = 104
quality = 7
```

这条是替代位窗里目前最像真的一条之一，但它落在 `eq=11`，更像副手武器位，不足以解释项链/戒指/腰带/鞋/手套全面缺失的问题。

### 当前对替代位窗的结论

替代位窗 (`70 / 79 / 81`) 的意义在于：

- 它们证明 `code index` 起始位**可能不是单一固定值**
- 或者：当前 `header` 拆法只对部分 item 成立，另一些 item 的起点仍然没对准

但现阶段还不能直接采用这些位窗作为最终结论，因为：

- 它们虽然能打到 `vip/rin/tgl/wae`
- 可一旦继续读 body，经常马上出现明显不自洽的字段组合
- 说明“只改 code window，不改整体 item 起点/字段边界”还不够

## 当前已确认与未确认

### 已确认

- 文件顶层 section marker 与主要偏移
- header 的核心字段
- attributes 段的存在、编码方式与实际值
- `JM` 主区的存在、起点和 `item_count = 104`
- item header 中 `parent/equipped/code index` 这组位段方向基本正确

### 尚未完全确认

- `code index -> code` 的完整构造方式
- 每个 item 的精确起点和边界
- advanced item 扩展区的完整分支布局
- property list 是否完全使用传统 `0x1FF` 终止和 `Save Bits`
- 如何从 `unique/set/rare` 分支稳定还原出：
  - 矮人之星
  - 乌鸦之霜
  - 亮金项链
  - 蜘蛛网
  - 法师之拳
  - 军帽
  - 暴风之盾
  - 鞋子的真实 base code 与属性

## 下一步逆向重点

1. 继续利用“plausible body”筛选真实 item 起点，而不是只看 header 表面命中
2. 解释为什么部分候选点能正确命中 `7s8/6bs/g33`，而另一些槽位还对不上 `rin/amu/ulc/tgl/uit`
3. 在 `JM` 区中继续做“按已知装备清单反推”的定位：
   - `eq=2` 找项链
   - `eq=6/7` 找双戒指
   - `eq=8` 找腰带
   - `eq=9` 找鞋
   - `eq=10` 找手套
   - `eq=5/12` 找盾位
4. 在定位出真实 item 起点后，再展开 advanced item 扩展区和 property list
